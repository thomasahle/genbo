# Knob-planner retrodiction scorecard (Stage A)

Generated by `experiments/plan_knobs.py --retrodict` against `experiments/tuned_optima.md` (108 frontier rows). Grading: **exact** = ratio <= 1.25 (hops: equal), **step** = within one ladder step (ratio <= 2.5 / hops +-1), **miss** otherwise. UNKNOWN/INFERRED-flagged fields and walk-row cascade knobs are skipped. Honesty rule: every planner constant traces to a design law, the engine SearchPreset (main.rs:2514-2650), or a cited FINDINGS default -- nothing was fit to these rows.


## Per-knob agreement

| knob | scored | exact | step | miss | exact+step |
|---|---|---|---|---|---|
| mechanism | 108 | 105 | 0 | 3 | 97% |
| graph | 108 | 108 | 0 | 0 | 100% |
| p | 95 | 21 | 46 | 28 | 71% |
| tfloor | 85 | 17 | 39 | 29 | 66% |
| cascade_k | 68 | 12 | 35 | 21 | 69% |
| graph_m | 86 | 13 | 55 | 18 | 79% |
| kedge | 96 | 72 | 24 | 0 | 100% |
| hops | 92 | 45 | 25 | 22 | 76% |
| sq4_nav | 107 | 95 | 0 | 12 | 89% |
| sq4_int8k | 23 | 17 | 6 | 0 | 100% |
| resident_i8 | 108 | 48 | 0 | 60 | 44% |
| rerank_f16 | 108 | 21 | 0 | 87 | 19% |
| gamma_on | 53 | 53 | 0 | 0 | 100% |
| qseed_on | 108 | 108 | 0 | 0 | 100% |
| qseed_S | 14 | 6 | 8 | 0 | 100% |
| **all** | 1259 | 741 | 238 | 280 | 78% |

## Per-dataset agreement

| dataset | scored | exact | step | miss | exact+step |
|---|---|---|---|---|---|
| wiki-35M | 182 | 135 | 43 | 4 | 98% |
| DEEP-10M | 213 | 146 | 39 | 28 | 87% |
| WebVid-2.5M | 197 | 106 | 40 | 51 | 74% |
| msturing30m | 96 | 41 | 19 | 36 | 62% |
| cohere-1M | 50 | 19 | 5 | 26 | 48% |
| cohere-10M | 72 | 32 | 13 | 27 | 62% |
| t2i-1M | 166 | 103 | 26 | 37 | 78% |
| t2i-10M | 218 | 121 | 48 | 49 | 78% |
| t2i-100M | 65 | 38 | 5 | 22 | 66% |

## Systematic misses (same knob, same direction, >=3 rows) with diagnosis

### t2i-10M / resident_i8 (planner high, 18 rows)

- rows: r=0.8456 pred=True tuned=False, r=0.8776 pred=True tuned=False, r=0.8890 pred=True tuned=False, r=0.8969 pred=True tuned=False ...
- diagnosis: LAW GAP (acknowledged, DESIGN_LAWS 'Gaps' #2): footprint<=RAM/2 predicts RESIDENT everywhere, but the lever only measurably pays under memory pressure or at large anon footprints (wiki 35.8GB, P341; DEEP +27.6%, P346) and was null on msturing (P345). The descriptor lacks the pressure term; history omitted a (mostly) null lever. Neither side refuted: default-ON is harmless, but the planner cannot discriminate DEEP(+27.6%) from msturing(null) at similar d/footprint.

### t2i-10M / rerank_f16 (planner high, 18 rows)

- rows: r=0.8456 pred=True tuned=False, r=0.8776 pred=True tuned=False, r=0.8890 pred=True tuned=False, r=0.8969 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### t2i-1M / resident_i8 (planner high, 16 rows)

- rows: r=0.7583 pred=True tuned=False, r=0.8068 pred=True tuned=False, r=0.8492 pred=True tuned=False, r=0.8741 pred=True tuned=False ...
- diagnosis: LAW GAP (acknowledged, DESIGN_LAWS 'Gaps' #2): footprint<=RAM/2 predicts RESIDENT everywhere, but the lever only measurably pays under memory pressure or at large anon footprints (wiki 35.8GB, P341; DEEP +27.6%, P346) and was null on msturing (P345). The descriptor lacks the pressure term; history omitted a (mostly) null lever. Neither side refuted: default-ON is harmless, but the planner cannot discriminate DEEP(+27.6%) from msturing(null) at similar d/footprint.

### t2i-1M / rerank_f16 (planner high, 16 rows)

- rows: r=0.7583 pred=True tuned=False, r=0.8068 pred=True tuned=False, r=0.8492 pred=True tuned=False, r=0.8741 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### DEEP-10M / rerank_f16 (planner high, 14 rows)

- rows: r=0.8041 pred=True tuned=False, r=0.8334 pred=True tuned=False, r=0.8538 pred=True tuned=False, r=0.8682 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### WebVid-2.5M / rerank_f16 (planner high, 14 rows)

- rows: r=0.8062 pred=True tuned=False, r=0.9019 pred=True tuned=False, r=0.9024 pred=True tuned=False, r=0.9121 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### WebVid-2.5M / hops (planner low, 12 rows)

- rows: r=0.9019 pred=1 tuned=3, r=0.9024 pred=1 tuned=3, r=0.9188 pred=2 tuned=4, r=0.9322 pred=2 tuned=4 ...
- diagnosis: UNDERDETERMINED: reachability (recall-L4) solves hops from measured delta; ladders do not transfer (tuning-transfer taxonomy). Tuned deep-hop regimes (mst h3-8 at 2-line rows, WebVid h4-10 coverage-starved OOD, t2i tail h4-6) all exceed the engine 1/2/3 preset. DEEP/cohere/wiki match the preset. The delta pilot, not the descriptor, must set hops beyond the band default.

### DEEP-10M / tfloor (planner high, 11 rows)

- rows: r=0.9603 pred=1800 tuned=256, r=0.9720 pred=1800 tuned=450, r=0.9814 pred=1800 tuned=450, r=0.9907 pred=1800 tuned=450 ...
- diagnosis: ENGINE-vs-TUNED INCONSISTENCY surfaced: the engine's accurate d<=128 floor (1800, main.rs:2572) was calibrated on msturing's accurate band (tuned t=1800 at p64); DEEP's tuned accurate band is flat at 450 for p32..608 (P348/P353) -- the floor knob is containment-inert there (TINY-SCAN class: the scan seeds the beam, width does not bind). FINDING: one d-banded constant conflates two datasets whose binding stage differs; recall-L1's containment probe, not d, must set the floor. The planner deliberately mirrors the engine rather than patching the constant.

### WebVid-2.5M / cascade_k (planner low, 10 rows)

- rows: r=0.9024 pred=16 tuned=48, r=0.9188 pred=20 tuned=64, r=0.9383 pred=20 tuned=128, r=0.9415 pred=20 tuned=128 ...
- diagnosis: CONTAINMENT-PILOT ESCALATION branch of the P363 width law: on WebVid W=20 measurably loses containment (.0032/.0062, P363 verdict 'keep W32/48'), and the tail runs W128-256. The law names the escalation but its trigger is a measured pilot; descriptor-only prediction lands at the un-escalated 2k default.

### WebVid-2.5M / graph_m (planner low, 9 rows)

- rows: r=0.9019 pred=16 tuned=48, r=0.9024 pred=16 tuned=48, r=0.9188 pred=24 tuned=64, r=0.9322 pred=24 tuned=64 ...
- diagnosis: P364: 'M and p grow together with target recall' -- the laws state the direction but no constant; the engine preset (16/24/48) is flat per band. Tuned tails escalate M to 64-128 (WebVid/mst/t2i/wiki). UNDERDETERMINED: needs the joint-scaling schedule (P362 H=E+(R-1)B pilot), not a fit.

### msturing30m / resident_i8 (planner high, 8 rows)

- rows: r=0.7268 pred=True tuned=False, r=0.7900 pred=True tuned=False, r=0.8471 pred=True tuned=False, r=0.9109 pred=True tuned=False ...
- diagnosis: LAW GAP (acknowledged, DESIGN_LAWS 'Gaps' #2): footprint<=RAM/2 predicts RESIDENT everywhere, but the lever only measurably pays under memory pressure or at large anon footprints (wiki 35.8GB, P341; DEEP +27.6%, P346) and was null on msturing (P345). The descriptor lacks the pressure term; history omitted a (mostly) null lever. Neither side refuted: default-ON is harmless, but the planner cannot discriminate DEEP(+27.6%) from msturing(null) at similar d/footprint.

### msturing30m / rerank_f16 (planner high, 8 rows)

- rows: r=0.7268 pred=True tuned=False, r=0.7900 pred=True tuned=False, r=0.8471 pred=True tuned=False, r=0.9109 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### msturing30m / hops (planner low, 7 rows)

- rows: r=0.7268 pred=1 tuned=3, r=0.7900 pred=1 tuned=3, r=0.8471 pred=1 tuned=3, r=0.9483 pred=2 tuned=4 ...
- diagnosis: UNDERDETERMINED: reachability (recall-L4) solves hops from measured delta; ladders do not transfer (tuning-transfer taxonomy). Tuned deep-hop regimes (mst h3-8 at 2-line rows, WebVid h4-10 coverage-starved OOD, t2i tail h4-6) all exceed the engine 1/2/3 preset. DEEP/cohere/wiki match the preset. The delta pilot, not the descriptor, must set hops beyond the band default.

### WebVid-2.5M / p (planner low, 6 rows)

- rows: r=0.9523 pred=4 tuned=16, r=0.9626 pred=8 tuned=32, r=0.9677 pred=8 tuned=48, r=0.9724 pred=16 tuned=64 ...
- diagnosis: The miss-halving rung map assumes ~2x work per miss-halving (delta~0.5); WebVid is uniformly-hard OOD where probes buy little until seeds/graph saturate (routed-coverage regime, data-structure L3) -- tuned tail p rises 16..128 at nearly flat recall. delta must come from the 10-min coverage pilot.

### cohere-1M / sq4_nav (planner high, 6 rows)

- rows: r=0.9732 pred=True tuned=False, r=0.9814 pred=True tuned=False, r=0.9900 pred=True tuned=False, r=0.9925 pred=True tuned=False ...
- diagnosis: THE CITED CO-GATE EXCEPTION (P345): cohere d=768 = 12-line rows, lines-rule says SQ4, yet SQ4 is non-Pareto (-0.003 recall tax). cost-L1's precision co-gate (sqrt(m)-margin containment pilot) is a MEASURED quantity the descriptor cannot supply. The planner correctly emits SQ4 as PILOT-GATED candidate; without the pilot it mispredicts exactly this dataset. Law fine, descriptor insufficient.

### cohere-1M / resident_i8 (planner high, 6 rows)

- rows: r=0.9732 pred=True tuned=False, r=0.9814 pred=True tuned=False, r=0.9900 pred=True tuned=False, r=0.9925 pred=True tuned=False ...
- diagnosis: LAW GAP (acknowledged, DESIGN_LAWS 'Gaps' #2): footprint<=RAM/2 predicts RESIDENT everywhere, but the lever only measurably pays under memory pressure or at large anon footprints (wiki 35.8GB, P341; DEEP +27.6%, P346) and was null on msturing (P345). The descriptor lacks the pressure term; history omitted a (mostly) null lever. Neither side refuted: default-ON is harmless, but the planner cannot discriminate DEEP(+27.6%) from msturing(null) at similar d/footprint.

### cohere-1M / rerank_f16 (planner high, 6 rows)

- rows: r=0.9732 pred=True tuned=False, r=0.9814 pred=True tuned=False, r=0.9900 pred=True tuned=False, r=0.9925 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### cohere-10M / sq4_nav (planner high, 6 rows)

- rows: r=0.9461 pred=True tuned=False, r=0.9707 pred=True tuned=False, r=0.9832 pred=True tuned=False, r=0.9871 pred=True tuned=False ...
- diagnosis: THE CITED CO-GATE EXCEPTION (P345): cohere d=768 = 12-line rows, lines-rule says SQ4, yet SQ4 is non-Pareto (-0.003 recall tax). cost-L1's precision co-gate (sqrt(m)-margin containment pilot) is a MEASURED quantity the descriptor cannot supply. The planner correctly emits SQ4 as PILOT-GATED candidate; without the pilot it mispredicts exactly this dataset. Law fine, descriptor insufficient.

### cohere-10M / resident_i8 (planner high, 6 rows)

- rows: r=0.9461 pred=True tuned=False, r=0.9707 pred=True tuned=False, r=0.9832 pred=True tuned=False, r=0.9871 pred=True tuned=False ...
- diagnosis: LAW GAP (acknowledged, DESIGN_LAWS 'Gaps' #2): footprint<=RAM/2 predicts RESIDENT everywhere, but the lever only measurably pays under memory pressure or at large anon footprints (wiki 35.8GB, P341; DEEP +27.6%, P346) and was null on msturing (P345). The descriptor lacks the pressure term; history omitted a (mostly) null lever. Neither side refuted: default-ON is harmless, but the planner cannot discriminate DEEP(+27.6%) from msturing(null) at similar d/footprint.

### cohere-10M / rerank_f16 (planner high, 6 rows)

- rows: r=0.9461 pred=True tuned=False, r=0.9707 pred=True tuned=False, r=0.9832 pred=True tuned=False, r=0.9871 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### msturing30m / p (planner low, 5 rows)

- rows: r=0.9109 pred=3 tuned=16, r=0.9483 pred=6 tuned=32, r=0.9686 pred=11 tuned=64, r=0.9819 pred=22 tuned=160 ...
- diagnosis: The d<=128 reference-probe anchors (8/8/32, main.rs:2586) are DEEP-calibrated; msturing (clustered distribution, Kf=131072) needs 4-10x more scanned mass at matched recall in the mid/high band. delta and probe ladders do NOT transfer across datasets (tuning-transfer taxonomy); the scanned-mass Kf/n scaling cannot absorb a distribution-shape coverage deficit. Pilot required.

### t2i-1M / p (planner low, 5 rows)

- rows: r=0.9263 pred=1 tuned=4, r=0.9355 pred=1 tuned=4, r=0.9540 pred=10 tuned=40, r=0.9598 pred=3 tuned=8 ...
- diagnosis: The ladder usually CONTAINS the tuned value (t2i-1M balanced tops out at the tuned p40 rung; t2i-10M accurate hits 128/256/512 exactly) -- the misses are the rung-position map: miss-halving-per-doubling (delta=0.5) under-places OOD datasets whose miss floor m_inf is high, so late rungs buy less than 2x (coverage saturation, P253/P255 delta up to 0.72). Rung placement needs the measured saturation delta, not a constant.

### t2i-100M / tfloor (planner low, 5 rows)

- rows: r=0.8796 pred=500 tuned=8000, r=0.8951 pred=500 tuned=8000, r=0.9049 pred=500 tuned=8000, r=0.9152 pred=1000 tuned=8000 ...
- diagnosis: SCALE-GATE: the capped sqrt(n/10M) floor scaling (P364) assumes probes shrink ~1/n above 10M so scanned mass stays constant; on t2i-100M the 3-level router (hierk3) kept tuned p at 40-104 (not ~10x smaller), so scanned mass and the tuned floor (8000) grew. Extrapolation to 100M was never claimed (P240 wash; role-reallocation L5 scale gate). Pilot territory.

### t2i-100M / graph_m (planner low, 5 rows)

- rows: r=0.8796 pred=16 tuned=64, r=0.8951 pred=16 tuned=64, r=0.9049 pred=16 tuned=64, r=0.9152 pred=24 tuned=64 ...
- diagnosis: P364: 'M and p grow together with target recall' -- the laws state the direction but no constant; the engine preset (16/24/48) is flat per band. Tuned tails escalate M to 64-128 (WebVid/mst/t2i/wiki). UNDERDETERMINED: needs the joint-scaling schedule (P362 H=E+(R-1)B pilot), not a fit.

### t2i-100M / resident_i8 (planner high, 5 rows)

- rows: r=0.8796 pred=True tuned=False, r=0.8951 pred=True tuned=False, r=0.9049 pred=True tuned=False, r=0.9152 pred=True tuned=False ...
- diagnosis: LAW GAP (acknowledged, DESIGN_LAWS 'Gaps' #2): footprint<=RAM/2 predicts RESIDENT everywhere, but the lever only measurably pays under memory pressure or at large anon footprints (wiki 35.8GB, P341; DEEP +27.6%, P346) and was null on msturing (P345). The descriptor lacks the pressure term; history omitted a (mostly) null lever. Neither side refuted: default-ON is harmless, but the planner cannot discriminate DEEP(+27.6%) from msturing(null) at similar d/footprint.

### t2i-100M / rerank_f16 (planner high, 5 rows)

- rows: r=0.8796 pred=True tuned=False, r=0.8951 pred=True tuned=False, r=0.9049 pred=True tuned=False, r=0.9152 pred=True tuned=False ...
- diagnosis: PLANNER-vs-STALE-TUNING: recall-L2's simplification (P350) makes fp16+12-row f32 correction the unconditional final tier; it was measured only on DEEP (+14.5-35.4% QPS at exact recall) and adopted on wiki. All other frontier rows predate P350. Verdict: the historical tuning is (per the law) leaving QPS on the table off wiki/DEEP -- but the law's universality is UNTESTED there. Action: apply-and-measure (cheap), or scope the law to d-regimes where the rerank tier is line-bound.

### cohere-1M / tfloor (planner low, 4 rows)

- rows: r=0.9732 pred=500 tuned=3000, r=0.9814 pred=500 tuned=4000, r=0.9900 pred=500 tuned=6000, r=0.9967 pred=500 tuned=12000
- diagnosis: UNDERDETERMINED (graph-free) + missing p-coupling: cohere's cited t=31.25p law (P295) sets tfloor from p, not from the d-banded floor. The engine floor has no p term; single-dataset anchor, not generalized.

### cohere-10M / cascade_k (planner high, 4 rows)

- rows: r=0.9707 pred=48 tuned=16, r=0.9832 pred=48 tuned=16, r=0.9871 pred=48 tuned=16, r=0.9904 pred=48 tuned=16
- diagnosis: CONSERVATIVE-HIGH against a near-inert knob: the accurate 4.8k width exists to 'leave room for the WebVid-style binding cases' (engine comment, main.rs:2545-2554); cohere's margins are wide (P363 W16 arms reproduce the P304/P299 recalls exactly), so the tuned W=16 loses no containment and the pilot would settle at ~1.6k. Cost of the planner's 48: a slightly wider exact-rerank tier, no recall change. Law fine; the escalation trigger (measured containment) is absent from the descriptor in both directions.

### t2i-10M / p (planner low, 4 rows)

- rows: r=0.9245 pred=20 tuned=56, r=0.9567 pred=40 tuned=128, r=0.9675 pred=64 tuned=192, r=0.9850 pred=128 tuned=384
- diagnosis: The ladder usually CONTAINS the tuned value (t2i-1M balanced tops out at the tuned p40 rung; t2i-10M accurate hits 128/256/512 exactly) -- the misses are the rung-position map: miss-halving-per-doubling (delta=0.5) under-places OOD datasets whose miss floor m_inf is high, so late rungs buy less than 2x (coverage saturation, P253/P255 delta up to 0.72). Rung placement needs the measured saturation delta, not a constant.

### t2i-10M / tfloor (planner low, 4 rows)

- rows: r=0.9922 pred=3000 tuned=8000, r=0.9927 pred=3000 tuned=8000, r=0.9939 pred=3000 tuned=10000, r=0.9945 pred=3000 tuned=12000
- diagnosis: SYSTEMATIC: within-band (esp. accurate) tuned tfloor grows with p (wiki ~45p at tail, mst ~20p, WebVid ladder 300..2000; cohere 31.25p) while the engine floor is flat per band -- yet DEEP is genuinely flat (450 for p32..608). The floor needs a dataset-coefficient p-coupling term (c_t from a containment pilot); a global constant would be an overfit. FINDING: law needs a term, engine preset is the miss on 3 of 5 graph datasets.

### msturing30m / mechanism (planner walk, 3 rows)

- rows: r=0.7268 pred=round_walk(PILOT rho*E_s/E_a) tuned=cascade, r=0.7900 pred=round_walk(PILOT rho*E_s/E_a) tuned=cascade, r=0.8471 pred=round_walk(PILOT rho*E_s/E_a) tuned=cascade
- diagnosis: THE STANDING COUNTEREXAMPLE (cost-L2): msturing d=100 sits in the walk byte-regime yet the cascade wins every band vs bug-fixed Roar (P339/P341). Architecture is a pilot inequality (rho x E_s/E_a), not a dimension rule -- the planner emits 'walk(PILOT)' and the pilot would refuse it here.

### cohere-1M / cascade_k (planner high, 3 rows)

- rows: r=0.9814 pred=48 tuned=16, r=0.9925 pred=48 tuned=16, r=0.9971 pred=48 tuned=16
- diagnosis: CONSERVATIVE-HIGH against a near-inert knob: the accurate 4.8k width exists to 'leave room for the WebVid-style binding cases' (engine comment, main.rs:2545-2554); cohere's margins are wide (P363 W16 arms reproduce the P304/P299 recalls exactly), so the tuned W=16 loses no containment and the pilot would settle at ~1.6k. Cost of the planner's 48: a slightly wider exact-rerank tier, no recall change. Law fine; the escalation trigger (measured containment) is absent from the descriptor in both directions.

### cohere-10M / tfloor (planner low, 3 rows)

- rows: r=0.9871 pred=1000 tuned=3000, r=0.9904 pred=1000 tuned=4000, r=0.9912 pred=1000 tuned=4000
- diagnosis: Same t=31.25p coupling (P295): tuned floor grows with p inside the accurate band (2000..4000) while the engine floor is flat per band (1000). Band-Level Adaptivity law says stages scale JOINTLY -- the flat per-band floor is the missing term.

### t2i-10M / hops (planner low, 3 rows)

- rows: r=0.9922 pred=3 tuned=6, r=0.9939 pred=3 tuned=5, r=0.9945 pred=3 tuned=5
- diagnosis: UNDERDETERMINED: reachability (recall-L4) solves hops from measured delta; ladders do not transfer (tuning-transfer taxonomy). Tuned deep-hop regimes (mst h3-8 at 2-line rows, WebVid h4-10 coverage-starved OOD, t2i tail h4-6) all exceed the engine 1/2/3 preset. DEEP/cohere/wiki match the preset. The delta pilot, not the descriptor, must set hops beyond the band default.


## Non-systematic misses (<3 rows; listed, not diagnosed)

- cohere-10M / p (high): r=0.9461 pred=64 tuned=16; r=0.9871 pred=320 tuned=96
- cohere-1M / p (low): r=0.9814 pred=40 tuned=128
- DEEP-10M / p (low): r=0.9265 pred=4 tuned=20; r=0.9426 pred=8 tuned=24
- DEEP-10M / resident_i8 (high): r=0.9987 pred=True tuned=False
- msturing30m / cascade_k (high): r=0.9686 pred=48 tuned=16; r=0.9883 pred=48 tuned=16
- msturing30m / cascade_k (low): r=0.9819 pred=48 tuned=128
- msturing30m / p (high): r=0.7268 pred=3 tuned=1
- msturing30m / tfloor (low): r=0.9883 pred=1800 tuned=7000
- t2i-100M / p (low): r=0.9152 pred=16 tuned=80; r=0.9233 pred=16 tuned=104
- t2i-10M / graph_m (low): r=0.9939 pred=48 tuned=128; r=0.9945 pred=48 tuned=128
- wiki-35M / cascade_k (high): r=0.9609 pred=48 tuned=16
- wiki-35M / graph_m (low): r=0.9505 pred=24 tuned=64; r=0.9590 pred=24 tuned=64
- wiki-35M / tfloor (low): r=0.9925 pred=1000 tuned=4000

## Knobs the laws genuinely cannot set yet (declared, not fit)

- **Walk-mode R/B/W (round/frontier/width)**: P356's grid + H=E+(R-1)B give the accounting, not the values; the delta pilot is the bridge. Not scored.
- **hops beyond the band preset**: needs measured delta (recall-L4); mst h3-8 / WebVid h4-10 / t2i tail h4-6 are pilot territory.
- **Graph-free p and TFLOOR**: single anchor (cohere P304 ladder / P295 t=31.25p); no transferable constant.
- **TFLOOR p-coupling coefficient inside a band**: direction cited (jointly-scaled stages), constant dataset-specific (31.25p cohere, ~45p wiki, ~20p mst, flat DEEP).
- **GRAPH_M tail schedule**: 'M and p grow together' (P364) has no quantitative form; preset anchors 16/24/48 only.
- **SQ4 co-gate verdict**: containment pilot (GATE1-style) is a measurement; descriptor cannot predict cohere-10M's fail vs wiki's pass.
- **CASCADE_K containment escalation**: the P363 law names the branch (WebVid W>=32); the trigger is a measured containment loss.
- **RESIDENT at small footprints**: pays on DEEP (+27.6%), null on msturing -- pressure term missing (acknowledged law gap #2).
- **gamma value**: OOD on/off is law-set; the scalar (0.5 t2i / 0.2 WebVid) is a per-dataset calibration fit by design.
- **SQ4_INT8K constants**: cited per-regime defaults (256/512 d>=1024; 192/384 d=512); no scaling law.
- **nav 'lowrank' tier**: no law anchor exists; the planner never emits it.


## Honesty notes

- The rung-within-band map (miss halves per work doubling) is the one place a modeling choice was needed; delta=0.5 is stated as an approximation of the measured 0.44-0.72 range (recall-L4), chosen a priori, not tuned to the rows.
- Where a systematic miss says PLANNER-vs-STALE-TUNING (fp16 tier), the claim is falsifiable: apply the lever to one non-DEEP/wiki frontier point and measure.
- Scoring counts msturing cascade_k disagreements even though P363 proved the knob inert there (W16==W256); see its diagnosis.
- Two ENGINE-vs-TUNED inconsistencies were surfaced rather than patched: the accurate d<=128 floor (1800 fits msturing, DEEP's tuned tail is flat 450) and the accurate 4.8k width (provision for binding cases; inert-wide on cohere/msturing). The planner mirrors the engine so the disagreement stays visible.
- Per-dataset COMMON flag tables are transcribed from tuned_optima.md's Notes blocks (stack lines), since flags live in prose, not the knob column.

