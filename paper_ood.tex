\documentclass{article}
% Empirical / methods paper (SEPARATE from the theory paper.tex, which is the
% cumulative-score-and-ball proof paper). Target: NeurIPS main track.
% STRUCTURE: a bottleneck-localization spine (OOD ANN is routing/coverage-bound,
% not scan/code-bound) with a MEASURED COLLECTION of ideas hanging off it -- the
% positives attack routing/coverage, the negatives (scan/code-side) are evidence
% for the thesis. Every idea reports its own quantified Effect. Swap \documentclass
% to neurips_2026 at submit time.
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}
\SetKwProg{Proc}{Procedure}{:}{}
\SetKwFunction{Route}{Route}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta}
\newcommand{\todo}[1]{{\color{red}[TODO: #1]}}
\newcommand{\pending}[1]{{\color{blue}[pending: #1]}}
\newcommand{\effect}[1]{\par\smallskip\noindent\textbf{Effect.} #1}
\setlength{\emergencystretch}{3em} % absorb stray overfull boxes in this draft

\title{Localizing and Beating the Out-of-Distribution Bottleneck in Vector Search:\\
A Measured Collection of Routing, Coverage, and Systems Techniques}
\author{\todo{authors}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Scalable approximate nearest-neighbor (ANN) search routes a query to a few
partition cells, scans quantized codes inside them, and re-ranks a shortlist.
On \emph{out-of-distribution} (OOD) workloads --- where queries come from a
different distribution than the database, as in cross-modal (text$\to$image)
retrieval under inner product --- we first \emph{localize the bottleneck}: a battery
of scan-side and code-side improvements (wider SIMD, higher-resolution and rotated
codes, RaBitQ, ADC routing) leave OOD recall/QPS essentially unmoved, while the
achievable recall is capped by which cells get \emph{routed to and covered}. We
then present a collection of routing- and coverage-directed techniques, each
measured in isolation: a one-parameter routing recalibration ($\gamma$) grounded in
a cap-versus-ball selectivity argument; $k$-NN-graph coverage augmentation; a
joint cross-level partition optimization; multi-assignment; and a corollary that
\emph{reverses} standard practice --- once graph coverage is present, coarser cells
win. Composed and evaluated under a same-hardware, tight-pairwise protocol against
ScaNN at its official configuration, the engine attains higher QPS at matched
recall@10 at 1M, 10M\pending{, and 100M}, and transfers to the streaming track.
We report the negative results as first-class evidence for the localization, and
treat the measurement methodology --- which repeatedly caught mirages during
development --- as a contribution.
\end{abstract}

\section{Introduction}
\label{sec:intro}
\todo{Motivate OOD vector search (RAG, cross-modal). The IVF+quant pipeline:
route$\to$scan$\to$re-rank. Thesis sentence: on OOD the binding constraint is
ROUTING/COVERAGE, not scan throughput or code fidelity -- and we show this by
measuring both sides. Then: a collection of coverage/routing techniques, each with
its own measured effect, plus the informative negatives.}

\paragraph{Contributions (each measured in isolation).}
\begin{itemize}
  \item \textbf{A bottleneck localization} (\S\ref{sec:localize}): scan- and
    code-side levers do not move OOD; routing/coverage does. The negatives
    (\S\ref{sec:neg}) are the evidence.
  \item \textbf{$\gamma$ routing recalibration} (\S\ref{sec:gamma}), grounded in a
    cap-vs-ball selectivity argument (\S\ref{sec:theory}). \emph{Effect: $\approx$
    half the probes at matched OOD recall, zero query-time cost.}
  \item \textbf{$k$-NN-graph coverage} (\S\ref{sec:graph}). \emph{Effect: composes
    with $\gamma$; at 1M moves the ScaNN ratio $1.18\to0.98$.}
  \item \textbf{The coarse-cell corollary} (\S\ref{sec:coarse}). \emph{Effect: with
    graph coverage present, coarser cells win; at 10M $1.01\to0.85$.}
  \item \textbf{Joint cross-level partition optimization (tree-EM)}
    (\S\ref{sec:em}). \emph{Effect: $+0.6$--$1.0$ recall@10 points at 10M, QPS-neutral.}
  \item \textbf{Multi-assignment (SOAR)} (\S\ref{sec:soar}). \emph{Effect: raises the
    routing recall ceiling to baseline parity.}
  \item \textbf{Systems levers for streaming} (\S\ref{sec:stream}): batch-parallel
    insert (\emph{$4.5\times$}), low-live adaptive $p$ (\emph{worst step
    $0.74\to0.94$}), and a parallelization fix (\emph{$7.6\times$ at 8 threads}).
  \item \textbf{A same-hardware evaluation methodology} (\S\ref{sec:method}) and an
    honest scope of what the ratios do and do not claim.
\end{itemize}

\section{Background: Standard Building Blocks}
\label{sec:bg}
Our system is assembled from well-established components; we summarize them here so
the contributions in \S\ref{sec:overview}--\S\ref{sec:soar} can be stated as deltas
against known practice.

\paragraph{Inverted file (IVF).} Partition the database into $C$ cells (the Voronoi
regions of $C$ centroids). At query time, \emph{route} the query to its $p\!\ll\!C$
nearest centroids and scan only those cells. End-to-end recall is capped by whether
the true neighbor's cell is among the $p$ probed --- the routing question that this
paper argues is the OOD bottleneck.

\paragraph{Hierarchical routing.} Scoring all $C$ centroids per query is $O(Cd)$,
which dominates at scale (fine $C$ is needed to keep cells small). A \emph{tree} of
centroids --- coarse $k$-means over $C_0$ centroids, then fine $k$-means within each
--- reduces routing to $O(L\,C^{1/L})$ for an $L$-level tree (FAISS uses an
HNSW graph over the centroids for the same purpose). We use hierarchical $k$-means.

\paragraph{Product quantization (PQ).} Split each vector's $d$ dimensions into $m$
subspaces and quantize each subspace to a small codebook ($16$ codewords for
$4$-bit PQ). Approximate distances are computed by lookup-and-add over per-subspace
tables (ADC). With $4$-bit codes the table lookup is a register shuffle
(\texttt{vpshufb}), enabling the SIMD ``fast-scan'' kernels (Quick-ADC, ScaNN, FAISS
FastScan). \emph{Anisotropic} / score-aware quantization (ScaNN) weights the
quantization loss to preserve inner-product ranking rather than reconstruction.

\paragraph{Exact re-ranking.} The quantized scan is used only to select a shortlist;
exact distances (int8 or float, read for the few shortlisted vectors) re-order it.
The shortlist depth $t$ trades recall for cost.

\paragraph{Multi-assignment.} Replicating each point into its top-$a_0$ cells (a
space/time tradeoff) raises the chance a query's neighbor lands in a probed cell.

\paragraph{The OOD workload.} The big-ANN OOD track (text2image) pairs
\emph{image}-embedding base vectors ($d{=}200$) with \emph{text}-embedding queries
under inner product, with a float ground truth. Base norms are concentrated but not
constant ($\approx0.81$--$0.99$); queries are unit-norm. This distribution gap
between base and queries is what breaks $L_2$-trained routing (\S\ref{sec:gamma}).

\section{System Overview}
\label{sec:overview}
% The complete pipeline, so the per-idea sections have a home. Table~\ref{tab:oldnew}
% marks standard blocks vs our contributions.
Figure~\ref{fig:arch} shows the pipeline; we describe the build
and query paths, then delineate standard vs.\ contributed components
(Table~\ref{tab:oldnew}).

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=5mm and 8mm,
  box/.style={draw, rounded corners, align=center, minimum height=9mm,
    font=\small, inner sep=3pt},
  idx/.style={draw, dashed, rounded corners, align=center, font=\footnotesize,
    fill=black!4, inner sep=3pt},
  ar/.style={-{Latex}, thick},
  lbl/.style={font=\scriptsize, midway, above}
]
% query path (top row)
\node[box] (q) {query\\$q,\,q_f$};
\node[box, right=of q] (route) {\textbf{Route}\\tree descent};
\node[box, right=of route] (scan) {fast-scan\\PQ codes};
\node[box, right=of scan] (casc) {int8 cascade\\prune};
\node[box, right=of casc] (rr) {float rerank\\$+$ graph};
\node[box, right=of rr] (out) {top-10};
\draw[ar] (q)--(route);
\draw[ar] (route)--node[lbl]{$p$}(scan);
\draw[ar] (scan)--node[lbl]{$t$}(casc);
\draw[ar] (casc)--node[lbl]{$K$}(rr);
\draw[ar] (rr)--(out);
% index (bottom row), built offline
\node[idx, below=13mm of route] (tree) {hier.\ $k$-means\\tree $+\,\gamma$ bias};
\node[idx, below=13mm of scan] (pq) {PQ blocks\\$+$ raw int8};
\node[idx, below=13mm of rr] (g) {$k$-NN graph\\$+$ float base};
\draw[ar, dashed] (tree)--(route);
\draw[ar, dashed] (pq)--(scan);
\draw[ar, dashed] (g)--(rr);
\node[font=\footnotesize\itshape, left=3mm of tree] {index};
\end{tikzpicture}
\caption{Query pipeline (top) and the offline-built index it consumes (bottom,
dashed). Edge labels are the shrinking candidate set: $p$ probed leaves $\to$ top-$t$
pool $\to$ $K$ cascade survivors $\to$ top-10. $\gamma$ calibration, the $k$-NN graph
union, and the coarse-cell tree geometry are this work's contributions
(Table~\ref{tab:oldnew}).}
\label{fig:arch}
\end{figure}

\paragraph{Build path.}
\begin{enumerate}
  \item \textbf{Quantize.} Float base $\to$ int8 via a single global scale
    ($127/\max|x|$); the int8 base is the native scan/route representation.
  \item \textbf{Train the routing tree.} Hierarchical $k$-means: $C_0$ coarse
    centroids, then fine centroids within each coarse cell to $K_f$ leaves total
    (2-level; generalizes to $L$ levels).
  \item \textbf{Joint refinement (tree-EM, \S\ref{sec:em}).} Optionally re-optimize
    all levels jointly (E: reassign sample points to their nearest leaf via the beam
    descent; M: recompute every level's centroids).
  \item \textbf{Multi-assign (SOAR, \S\ref{sec:soar}).} Assign each point to $a_0$
    leaves with spill that covers the residual direction of the first.
  \item \textbf{Encode.} $4$-bit anisotropic PQ ($m{=}d/2$ subspaces); pack codes
    into cell-contiguous $16$-row blocks laid out for the \texttt{vpshufb} fast-scan.
    Store the raw int8 vectors in cell order too (cache-warm re-rank gathers).
  \item \textbf{Calibrate \& augment.} Precompute the per-cell $\gamma$ routing bias
    (\S\ref{sec:gamma}); build the base-side $k$-NN graph sidecar (\S\ref{sec:graph}).
\end{enumerate}

\paragraph{Query path.}
\begin{enumerate}
  \item \textbf{Route (tree descent).} Score the $C_0$ coarse centroids (int8, VNNI
    kernel), keep the top-$b_0$; expand to their fine children, score those with the
    $\gamma$ bias added, and return the top-$p$ leaves.
  \item \textbf{Scan.} Fast-scan the $4$-bit PQ codes of the $p$ probed cells
    (\texttt{vpshufb} ADC with int16 accumulation for $\sim$12-bit selection
    resolution; optional AVX-512 64-wide), collecting a bounded top-$t$ pool.
  \item \textbf{Cascade re-rank.} Deduplicate the pool by original id (needed under
    multi-assignment); exact-rescore the $t$ survivors in int8 (VNNI dot); prune to
    the top-$K$ ($K{=}16$).
  \item \textbf{Float re-rank + graph union.} Compute exact float inner products for
    the $K$ survivors (reading the original float vectors) unioned with the $k$-NN
    graph neighbors of the top-$M$; return the top-10.
\end{enumerate}

Algorithms~\ref{alg:build}--\ref{alg:query} state the pipeline precisely. All
routing scores use the calibrated form $s_\gamma(q,c)=\gamma\lVert c\rVert^2-2\,
q\!\cdot\!c$ (\S\ref{sec:gamma}); the constant $\lVert q\rVert^2$ is dropped, and
smaller $s_\gamma$ means a better cell. Leaf centroids are $\{c_j\}_{j=1}^{K_f}$
with precomputed bias $b_j=(\gamma-1)\lVert c_j\rVert^2$ so that
$s_\gamma(q,c_j)=\lVert c_j\rVert^2-2q\!\cdot\!c_j+b_j$ is one dot product plus an
additive constant.

\begin{algorithm}[t]
\caption{Build}
\label{alg:build}
\KwIn{float base $X_f\!\in\!\mathbb{R}^{n\times d}$; tree sizes $C_0,K_f$;
  multi-assign $a_0$; calibration $\gamma$; EM rounds $R$; graph degree $k$}
\KwOut{index $(\{Q_0\},\{c_j\},b,\text{PQ blocks},\text{raw},G)$}
$\sigma \gets 127/\max_i\lVert X_f[i]\rVert_\infty$;\ \ $X \gets \mathrm{int8}(\sigma X_f)$
  \tcp*{global-scale quantize}
$\{Q_0\}\gets k\text{-means}(X,C_0)$;\ \ $\{c_j\}\gets$ fine $k$-means within each
  coarse cell to $K_f$ leaves\;
\For{$R$ rounds \tcp*[f]{tree-EM (\S\ref{sec:em}); optional}}{
  \textbf{E:} assign each sample to its nearest leaf via \Route{$\cdot$,\,1}\;
  \textbf{M:} recompute $\{Q_0\},\{c_j\}$ from the assignments\;
}
\ForEach{point $x_i$ \tcp*[f]{SOAR multi-assign (\S\ref{sec:soar})}}{
  assign $x_i$ to its top-$a_0$ leaves; spill covers the residual of the 1st\;
}
per leaf: encode members as 4-bit anisotropic PQ, pack into 16-row \texttt{vpshufb}
  blocks; store raw int8 in leaf order\;
$b_j \gets (\gamma-1)\lVert c_j\rVert^2$ for all $j$ \tcp*{routing bias}
$G \gets$ base-side $k$-NN graph (self-search; \S\ref{sec:graph})\;
\Return index $(\{Q_0\},\{c_j\},b,\text{PQ blocks},\text{raw},G)$\;
\end{algorithm}

\begin{algorithm}[t]
\caption{Route (tree descent) \textendash{} returns the $p$ best leaves}
\label{alg:route}
\Proc{\Route{$q$, $p$}}{
  $S_0 \gets$ top-$b_0$ coarse cells minimizing $s_\gamma(q,Q_0)$
    \tcp*{int8/VNNI kernel}
  $\mathcal{L}\gets$ fine children of $S_0$\;
  score each $c_j\!\in\!\mathcal{L}$ by $\lVert c_j\rVert^2-2q\!\cdot\!c_j+b_j$\;
  \Return the $p$ leaves of $\mathcal{L}$ with smallest score\;
}
\end{algorithm}

\begin{algorithm}[t]
\caption{Query}
\label{alg:query}
\KwIn{int8 query $q$, float query $q_f$; probes $p$; pool depth $t$; prune $K$;
  graph fan-in $M$}
$\mathcal{C}\gets$ \Route{$q,p$}\;
$\mathcal{P}\gets\emptyset$ \tcp*{bounded top-$t$ pool}
\ForEach{cell $\in\mathcal{C}$}{
  fast-scan its 4-bit PQ codes (\texttt{vpshufb} ADC, int16 accum) $\to$ approx.\ scores\;
  keep the $t$ best in $\mathcal{P}$ (threshold-bounded collect)\;
}
$\mathcal{P}\gets$ dedup $\mathcal{P}$ by original id \tcp*{needed under $a_0\!>\!1$}
$\mathcal{K}\gets$ top-$K$ of $\mathcal{P}$ by exact int8 IP (VNNI dot) \tcp*{cascade prune}
$\mathcal{R}\gets \mathcal{K}\cup\bigcup_{r\in\text{top-}M(\mathcal{K})} G[r]$
  \tcp*{$k$-NN-graph coverage union}
\Return top-10 of $\mathcal{R}$ by exact float IP $\langle q_f, x_r\rangle$\;
\end{algorithm}

\begin{table}[t]
\centering
\caption{Components of the pipeline: standard building blocks vs.\ this work.}
\label{tab:oldnew}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lll}
\toprule
Component & Basis & Our delta \\
\midrule
int8 IVF partition        & standard              & --- \\
Hierarchical $k$-means tree & standard (FAISS-style) & fast int8/VNNI descent kernel \\
$4$-bit anisotropic PQ    & ScaNN / FastScan family & int16-resolution + 32/64-wide fast-scan \\
Multi-assignment (SOAR)   & ScaNN-derived         & dedup-before-cap correctness fix \\
int8$\to$float re-rank cascade & folklore         & the specific $K{=}16$ composition \\
$\gamma$ routing calibration & \textbf{ours}      & the OOD miscalibration fix \\
$k$-NN-graph coverage union  & \textbf{ours}      & composition with $\gamma$ + float re-rank \\
Coarse-cell geometry knee    & \textbf{ours}      & reverses ``finer is better'' \\
Tree-EM joint optimization   & \textbf{ours}      & cross-level partition objective \\
Batch-insert / adaptive-$p$ (streaming) & \textbf{ours} & \S\ref{sec:stream} \\
\bottomrule
\end{tabular}}
\end{table}

\section{Theoretical Grounding (Scoped)}
\label{sec:theory}
% Honest scope: we use the primitive-selection argument and two design principles
% from the cumulative-score-and-ball line of work; we do NOT invoke its polylog-loss
% recursive-tree result, which does not pay off at the scales evaluated here.
\todo{(i) Cap-vs-ball under concentration: additive ball filters are non-selective
in high dimension (everything within $R+r$), angular/spherical-cap filters remain
selective; $k$-means on centered data realizes a data-dependent angular partition.
(ii) This frames $\gamma$: the routing score $\gamma\lVert c\rVert^2-2q\cdot c$
interpolates $\gamma{=}1$ (additive ball / $L_2$) to $\gamma{\to}0$ (pure inner
product / cap), so the theory predicts $\gamma{<}1$ is needed precisely in the
concentrated OOD regime -- which we confirm. (iii) Two principles we build on:
route-precise/rank-cheap, and multi-assignment as a space/time tradeoff. Explicitly
state we do NOT claim the recursive-tree polylog result (refuted at $\le$10M scale).}

\section{Localizing the OOD Bottleneck}
\label{sec:localize}
\todo{Frame the diagnostic: pool-recall ceiling (re-rank the whole probed pool) vs
scan-limited recall. Show scan is already extracting the pool (no scan headroom),
so recall is set by routing/coverage. This motivates attacking routing (\S5-9) and
predicts scan/code levers won't help (\S\ref{sec:neg}, the negatives).}

\section{$\gamma$: One-Parameter Routing Recalibration}
\label{sec:gamma}
\todo{Per-cell additive $i32$ bias $(\gamma{-}1)\lVert c\rVert^2$ precomputed at
build; zero per-query cost. Fit once per dataset ($\gamma\approx0.5$ on text2image,
1M and 10M). Ablate $\gamma\in\{0.4,0.45,0.5,0.55,0.6\}$.}
\effect{Reaches matched OOD recall at $\approx$half the probes ($p$: 54$\to$29 at
1M; $\approx$2.5$\times$ probe cut at 10M). Zero query-time cost.}

\section{$k$-NN-Graph Coverage Augmentation}
\label{sec:graph}
\todo{Union the graph neighbors of the top-$M$ shortlist members into the exact
re-rank set (base-side IP-kNN sidecar; built by ScaNN self-search or engine
self-query, $\sim$92\% edge agreement). Recovers deep neighbors a shallow probe
missed. Composes additively with $\gamma$ (partial overlap, compound $\sim0.97$).}
\effect{At 1M, composed with $\gamma$, the ScaNN ratio drops $1.18\to0.98$
(the first sub-1 result); at 10M it lets $t_{\text{surv}}$ halve at matched recall.}

\section{Routing Cost and the Coarse-Cell Corollary}
\label{sec:coarse}

\subsection{A routing cost model}
\label{sec:routecost}
Consider an $L$-level tree over $K_f$ leaves with cell counts
$C_0 < C_1 < \dots < C_{L-1} < C_L = K_f$ and per-level beam widths
$b_0,\dots,b_{L-2}$ (the number of cells expanded when descending from level $i$ to
level $i{+}1$). Routing a query costs, in centroid distance evaluations,
\begin{equation}
\label{eq:route}
\mathrm{route}(q) \;=\; C_0 \;+\; \sum_{i=1}^{L-1} b_{i-1}\,\frac{C_i}{C_{i-1}},
\end{equation}
since the coarse level scores all $C_0$ centroids and level $i$ expands the $b_{i-1}$
best parents from level $i{-}1$, each contributing $C_i/C_{i-1}$ children. The scan
then reads $\mathrm{scan}(q)=p\cdot(n/K_f)\cdot a_0$ candidates ($p$ probed leaves
$\times$ points/leaf $\times$ multi-assignment). For a 2-level tree \eqref{eq:route}
is $C_0 + b_0 K_f/C_0$, minimized at $C_0=\sqrt{b_0 K_f}$ to give
$2\sqrt{b_0 K_f}=O(\sqrt{K_f})$; with $L$ levels and balanced branching this falls to
$O(L\,K_f^{1/L})$. The two costs pull oppositely in $K_f$: finer leaves shrink the scan
($n/K_f$) but grow the routing. The beams $b_i$ are the ``routing aggression'' knob ---
wider beams retain the true cell with higher probability at linear cost --- and OOD
queries, which route poorly, need wider beams (\S\ref{sec:eval}).

Table~\ref{tab:geom} gives geometry and routing cost by scale. Two levels suffice
through 10M; at 100M a 2-level tree at its optimal $C_0=\sqrt{b_0 K_f}\approx16{,}400$
would cost $\approx\!33{,}000$ evals, whereas the 3-level tree roughly halves this
($14{,}336$) --- so the third level is introduced only at 100M, where routing is a
large enough share of the query to matter.

\begin{table}[t]
\centering
\caption{Tree geometry and per-query routing cost (Eq.~\ref{eq:route}) by scale.
Beams are aggressive ($b_0\!\approx\!12$--$25\%$ of coarse cells) because OOD queries
route poorly. \pending{100M validated via a scaling-law sweep (\S\ref{sec:routecost}).}}
\label{tab:geom}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccccc}
\toprule
scale & $L$ & $K_f$ & cell counts & beams & route evals & pts/leaf \\
\midrule
1M   & 2 & 16{,}384  & $C_0{=}768$              & $b_0{=}96$            & $768+96{\cdot}21=2{,}816$ & 61 \\
10M  & 2 & 65{,}536  & $C_0{=}4096$             & $b_0{=}128$           & $4096+128{\cdot}16=6{,}144$ & 152 \\
100M & 3 & 524{,}288 & $C_0{=}2048,C_1{=}32768$ & $b_0{=}512,b_1{=}256$ & $2048+512{\cdot}16+256{\cdot}16=14{,}336$ & 190 \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{The corollary}
\todo{Sweep cell granularity at 10M with the graph on: fine (Kf=262144), medium
(65536), coarse (32768); medium/coarse WIN. Bracket the knee with four builds.}
The cost model \eqref{eq:route} explains \emph{why}. Without graph coverage, recall
demands fine $K_f$ (small leaves make the true neighbor's leaf likelier to be among
the $p$ probed), paying $O(\sqrt{K_f})$ routing. The $k$-NN-graph union
(\S\ref{sec:graph}) supplies that coverage \emph{independently of $K_f$}, so the tree
can use \emph{coarser} leaves --- cheaper routing --- at fixed recall. The optimum is a
knee: too fine wastes routing, too coarse inflates the scan ($p\cdot n/K_f$).
Empirically the knee sits near 150 points/leaf at 10M, and both finer and coarser
builds lose to it.
\effect{At 10M, moving from the fine-cell to the 152-point-cell geometry takes the
ScaNN ratio $1.01\to0.85$; routing evals/query drop $\sim$12{,}000$\to$6{,}144.}

\section{Joint Cross-Level Partition Optimization (Tree-EM)}
\label{sec:em}
\todo{EM over the hierarchical partition: E-step reassigns to nearest leaf via the
beam descent (objective = query-time search), M-step recomputes every level jointly.
Cheap-beam E-step. Additive with $\gamma$/graph.}
\effect{$+0.6$--$1.0$ recall@10 points at 10M at matched probe, QPS-neutral
(build-time only).}

\section{Multi-Assignment (SOAR)}
\label{sec:soar}
\todo{Replicate each point into $a_0$ cells; SOAR spill covers the residual
direction of the first cell. A dedup fix (rerank by orig id before the survivor cap)
was required to realize it. Raises the routing coverage ceiling.}
\effect{$a_0{=}2$ reaches OOD coverage parity with the baseline partition; higher
$a_0$ extends the high-recall frontier at a storage/scan cost (quantified in table).}

\section{Systems Techniques (Streaming)}
\label{sec:stream}
\todo{msturing-30M-clustered final\_runbook; recall@10 under 1h + 8GB. Three levers:
(i) batch-parallel insert (assignment parallelized over the batch, order-preserving,
bit-identical); (ii) low-live adaptive $p$ (grow probes so expected candidates
$\ge$ threshold when the live set is small); (iii) a parallelization fix (the batched
re-rank driver ran chunks serially -> RAYON never engaged). Plus a compliance
re-architecture: first-insert-batch cold start + periodic live-set retraining.}
\effect{Batch insert: 30M inserts $1818\text{s}\to406\text{s}$ ($4.5\times$),
recall-identical. Adaptive $p$: worst step $0.737\to0.944$. Parallelization fix:
$7.6\times$ at 8 threads. Compliant recall@10 $=$ \pending{final NQ=10000},
peak $<$8GB.}

\section{Negative Results (Evidence for the Localization)}
\label{sec:neg}
% First-class: these are WHY we believe the bottleneck is routing/coverage.
\todo{Report each with its measured null/loss:}
\begin{itemize}
  \item \textbf{Query-aware routing} (train cells on the query distribution):
    \emph{worse} than base-trained ($0.76$ vs $0.89$ @ matched $p$) --- undertrained
    + the cells where queries are dense don't distribute base points well.
  \item \textbf{ADC routing} (4-bit-scan the centroids): recall-neutral but
    \emph{no speed win} --- the exact VNNI router is already at the compute floor.
  \item \textbf{Anisotropic/OPQ-rotated coarse codes for rank preservation}: do
    \emph{not} make coarse codes rank-preserving; reorder depth is set by code
    bit-rate, and rotation/$\eta$ move it $<0.01$. Coverage-via-coarsening collapses
    (a scann-coverage-matched dpb=50 config yields recall $0.09$).
  \item \textbf{RaBitQ}: no ranking edge over PQ at matched bytes.
  \item \textbf{Wider SIMD} (AVX-512 vpermw / 64-wide): scan is memory-bound, not
    shuffle-bound; $\le$+7\%, sometimes negative (downclock/cross-lane).
  \item \textbf{Finer cells at matched probe}: lose recall (half the probe fraction).
  \item \textbf{PCA/LeanVec dim reduction on OOD}: near-isotropic embeddings; dead.
\end{itemize}
\emph{These cluster on the scan/code side and all fail; the \S5--9 levers cluster on
routing/coverage and all succeed --- the localization made concrete.}

\section{Evaluation Methodology}
\label{sec:method}
We report \emph{same-hardware ratios}, never cross-machine QPS. All comparisons run
on one physical machine; each competitor is measured in the same session, seconds
apart, at its own best operating point.

\paragraph{Protocol.} For a target recall (recall@10 $\ge 0.90$), we (i) fix each
system's operating point by an independent sweep, requiring both to clear the recall
gate against the \emph{same} exact float ground truth; (ii) alternate the two systems
round to round (order flips) so neither owns a load window; (iii) take best-of-$k$
within a round and report the \emph{median of per-round ratios}; (iv) recompute
recall from raw ids, never trusting a cached number. On a shared machine, load drift
between two builds minutes apart exceeds the effects being measured, so only
interleaved same-window ratios and load-independent recall are trusted.

\paragraph{Warm-resident semantics; pristine confirmation.} Both systems measured
warm and resident (leaderboard serving); an early cold-spawn asymmetry is corrected
by scoring each engine's warm block. Headline numbers pause all other first-party
jobs (SIGSTOP) and confirm across $\ge$20 rounds.

\paragraph{The baseline at its best.} ScaNN uses its \emph{official} big-ANN recipe
(e.g.\ 40k leaves at 10M, anisotropic AH/LUT16, residual quant, bfloat16 re-order)
with its own leaves/re-order sweep including corner configs. A win depending on a
handicapped baseline is quarantined; we describe one (an under-leaved cached index)
and its correction.

\paragraph{What we do not claim.} Same-hardware ratios on one (shared) machine, not a
standardized-VM submission; ScaNN is the \emph{measured} opponent --- gaps to
binary-only leaderboard systems are \emph{inference} from the public leaderboard.

\section{Results}
\label{sec:eval}
\begin{table}[t]
\centering
\caption{OOD text2image, recall@10-matched QPS ratio (ScaNN$/$ours; $<1$ $=$ our
engine faster). Same-hardware, tight-pairwise, opponent at official config.
\pending{100M row}.}
\label{tab:main}
\begin{tabular}{lccc}
\toprule
Scale & 1-thread & 8-thread & recall@10 gate \\
\midrule
1M   & 0.755 & 0.756 & 0.905 \\
10M  & 0.827 & 0.833 & 0.901 \\
100M & \pending{} & \pending{} & \pending{} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Ablation ladder at 10M (ScaNN$/$ours ratio; each row adds one lever).}
\label{tab:ablation}
\begin{tabular}{lc}
\toprule
Configuration & ratio \\
\midrule
$\gamma$ only                         & 1.24 \\
$+$ graph coverage                    & 1.01 \\
$+$ tree-EM                           & 0.92 \\
$+$ coarse-cell geometry (knee)       & 0.85 \\
$+$ pristine confirmation (20 rounds) & 0.83 \\
\bottomrule
\end{tabular}
\end{table}
\todo{Add per-lever isolation deltas; 8-thread scaling; streaming table (recall,
peak mem, wall vs 1h budget). Note the ablation ordering is the actual development
arc.}

\section{Related Work}
\label{sec:rel}
\todo{ScaNN (anisotropic VQ), DiskANN/HNSW (graphs), FAISS IVF-PQ, big-ANN
challenge, RaBitQ. Position OOD-specific routing as the gap; distinguish $\gamma$
from residual quantization (orthogonal) and from query-aware training (shown to fail).}

\section{Limitations}
\label{sec:lim}
\todo{Single (shared) machine; $\gamma$ fit per dataset; the coarse-cell corollary is
demonstrated on IP/OOD data and may not transfer to in-distribution $L_2$; the gap to
binary-only leaderboard systems is inference; filter/sparse tracks untouched;
standardized-VM submission is future work and the gating experiment for a leaderboard
claim.}

\end{document}
